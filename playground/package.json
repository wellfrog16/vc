{
  "name": "playground",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "clean": "rimraf node_modules"
  },
  "devDependencies": {
    "@vue/tsconfig": "^0.8.1"
  }
}
